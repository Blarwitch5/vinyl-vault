---
export interface Props {
  maxWidth?: 'sm' | 'md' | 'lg' | 'xl' | '2xl' | 'full'
  padding?: 'none' | 'sm' | 'md' | 'lg'
  class?: string
}

const { maxWidth = 'xl', padding = 'md', class: className = '', ...rest } = Astro.props

// Largeurs maximales
const maxWidths = {
  sm: 'max-w-sm',
  md: 'max-w-md',
  lg: 'max-w-lg',
  xl: 'max-w-xl',
  '2xl': 'max-w-2xl',
  full: 'max-w-full',
}

// Padding
const paddings = {
  none: '',
  sm: 'px-4 py-2',
  md: 'px-6 py-4',
  lg: 'px-8 py-6',
}

const classes = `${maxWidths[maxWidth]} ${paddings[padding]} ${className}`
---

<div class={`mx-auto ${classes}`} {...rest}>
  <slot />
</div>
